# HANDOFF — EP-4 pathotype — fresh-session entry point — 2026-06-03

> Read this FIRST in a new session. Covers exact current state, the one next move, environment
> gotchas that bit us repeatedly, and copy-paste commands. Supersedes earlier EP-4 handoffs.

---

## 0. ⚠️ DO THIS FIRST — verify cwd (the recurring trap)

This session's binding pain all session was **wrong cwd**. `/project-state` and `/soraya minister`
are path-gated to the project root and REFUSE from anywhere else.

```
!pwd
```
- Must print `/c/Users/Farshad/PythonProjects/dna_decode`.
- If it prints `rca_engine/articles` (or anything else): you RESUMED a wrong-cwd session — resume
  restores the session's birth cwd regardless of where you typed `claude`. **Exit, `cd
  C:\Users\Farshad\PythonProjects\dna_decode`, `claude`, and pick NEW session (don't resume).**

Other environment facts:
- **D: external Seagate drive is FLAKY** — drops out mid-run (`WinError 21`), orphaned several runs.
  Route caches to **C:** (`data/...`, gitignored) not D:. `HF_HOME=C:/Users/Farshad/.cache/hf`.
- **venv:** `.venv/Scripts/python.exe` (uv; torch cu118, CUDA works on GTX 860M but weak/4GB).
  Deps via `uv pip install`. `PYTHONIOENCODING=utf-8` on python -c with unicode.
- **GPU jobs orphan on session boundaries** — anything multi-minute on the 860M must checkpoint
  per-unit or it loses everything. We deferred the NT track partly for this.
- **Docker Desktop daemon** must be running for Bakta/VF (start `C:\Program Files\Docker\Docker\Docker
  Desktop.exe`, ~30s). `blastn`/`kma` are NOT installed (needed for the VF-diff mission below).
- **Sync = git on `main`.** `data/` + `reports/` are gitignored → re-derive from public accessions,
  never rely on them syncing. Remote: github.com/mmdebrahimi/DNA_SEQUENCE_TO_TRAIT_PREDICTION.
  Tree is currently CLEAN + synced (HEAD ab71ea6).

---

## 1. Current state (one paragraph)

The **v0 pathotype resolver is shipped and ~95% done**: deterministic VirulenceFinder-marker
compatibility caller + abstention + provenance, FASTA→call CLI, **30/30 tests pass**, pushed. ExPEC
recall was hardened to an **honest 0.833 cross-axis strategy-cap** (we reverted a Soraya-minister
overfit that faked 0.917 by over-rescuing capsule-only JSPG on a lone serum gene — see §4). The
**modeling frontier hit a wall**: the ExPEC-vs-EPEC labeled cohort is **study==class confounded**
(ExPEC=Salipante lab, EPEC=Hazen lab), proven twice — the 0.729 separability probe = "batch not
biology" (commit 789d83d) and the k-mer baseline = **0.514 (chance)**. So any recall/AUROC number on
this substrate measures lab-of-origin, not biology → **recall-tuning is HALTED**; more modeling here
has ~zero info value. NT/embedding track DEFERRED (tet precedent + confound + 860M orphaning).

---

## 2. THE NEXT MOVE — VF side-by-side diff (completes v0)

Chosen by two converging Soraya analyses + user greenlight. It's the **last unfinished item of the
v0 originating goal** ("side-by-side comparison against CGE VirulenceFinder gene-call output").
Mission `ep4-v01-vf-diff` is **staged + schema-validated** (in_place, strategy-budget 2).

```
# from a dna_decode-rooted session (verified §0):
/soraya minister ep4-v01-vf-diff
```

What it does: PARKS round 1 on the interrogation gate (in_place gates EXECUTION not promotion) →
emits 2 design questions WITH Soraya's drafted answers (you ratify) → `--until-mvp` installs BLAST+
→ runs canonical VirulenceFinder over the FASTA → builds a `vf_diff` provenance section (resolver
cluster profile vs VF per-gene calls + concordance + honesty flag) → wires into cli.py → writes
`tests/test_pathotype_vf_diff.py` → greens both endpoints → AC9 ledger row.

**Two drafted interrogation answers (you RATIFY, don't author)** — full text in
`wiki/MINISTER_LAUNCH_ep4_v01_vf_diff_2026-06-03.md`:
- Q1 canonical-VF path → **native BLAST+ install** (matches the gate-A-proven `python -m
  virulencefinder` runtime; Docker VF is the heavier alternative).
- Q2 honesty bar → report **per-gene concordance + explicit `caller_is_independent_baseline: false`
  + caveat** that both callers use the same VF DB, so high concordance is expected (an AUDIT of the
  fast caller, NOT independent validation). Never a bare headline "agreement %". *(This Q secretly
  carries an authority decision about how honestly the tool represents its own non-independence —
  surfaced, not buried.)*

The ONLY friction is the BLAST+ install (dep-install; autonomous under money-only posture).

---

## 3. The locked sequence (don't skip steps)

1. **Complete v0 via VF diff** ← next move (§2).
2. **Freeze v0** as shipped + scope-capped (ledger already sequences this).
3. **Labels frontier** = get a **de-confounded** (within-study / lineage-matched) ExPEC+EPEC set.
   This is the REAL binding constraint (hypothesis H1 = label non-circularity, still
   under-investigation). It's a research/substrate problem (`/research` or targeted EnteroBase /
   within-study pull), NOT modeling. Until it exists, no embedding-vs-resolver comparison means
   anything.

**Explicitly NOT next:** ETEC 3-class expansion (needs full-GCA plasmid-toxin fetch — chromosome-only
fetch misses LT/ST, see commit f124b13); re-opening the confound by more tuning.

---

## 4. Why ExPEC recall is 0.833 not 0.917 (don't "fix" this)

The cross-axis rule is the rule of record and the 0.833 is intentional. A Soraya minister fast-path
shipped a flat-K=1 support rule (recall 0.917) that rescued capsule-only **JSPG** on a single
serum-resistance `traT` gene — the overfit the user pre-authorized refusing ("a clean 0.833 beats an
overfit 0.85"). Reverted to cross-axis (**≥1 iron gene AND ≥1 capsule gene**; `meets_cross_axis_support`
in `dna_decode/pathotype/expec_score.py`, `resolve_call(cross_axis_support=...)`). Result: JSMY
rescued (genuine 5-iron + serum), JSPG structurally excluded (iron=0), JSLG ambiguous (empty, no
lever). Precision 1.0, EPEC recall 1.0. Tests pin all of this (7/7 in
`tests/test_pathotype_expec_recall.py` incl JSPG structural-exclusion + overfit-canary). Ledger row
18 superseded by row 19. **Do not raise recall by re-rescuing JSPG.**

---

## 5. Ledger housekeeping (optional, do from dna_decode cwd)

`/project-state ecoli-pathotype-prediction-cli-2026-05-26` works once cwd is right. Two stale items:
- **Last Evaluation is ~5 days stale** (2026-05-29). Refresh:
  ```
  /project-state ecoli-pathotype-prediction-cli-2026-05-26 --update-last-evaluation \
    --progress-signal "v0 resolver shipped (30/30 tests); ExPEC recall hardened to honest 0.833 cross-axis strategy-cap; recall-tuning halted (cohort study==class confounded, k-mer 0.514); VF-diff mission staged to complete v0; labels frontier is the real next epoch"
  ```
- **~4 Pending Decisions rows are committed-shape but still labeled Pending** (Horesh-audit /
  Whittam / NCBI host_disease facet) — several now moot given the confound finding. Clean up with
  `--resolve-pending-decision <N> --resolution-note "..."` at the next framing-reset.

---

## 6. Key files

| Path | What |
|---|---|
| `project_state/ecoli-pathotype-prediction-cli-2026-05-26.md` | the ledger (Current state v6 = top of truth) |
| `project_state/ep4-v01-vf-diff/big-idea.md` | the staged VF-diff mission (schema-valid) |
| `wiki/MINISTER_LAUNCH_ep4_v01_vf_diff_2026-06-03.md` | launch handoff + full drafted interrogation answers |
| `dna_decode/pathotype/{resolve,detect,markers,expec_score,cli}.py` | the v0 resolver |
| `tests/test_pathotype_{resolver,expec_recall}.py` | 30 tests; run `uv run pytest tests/ -k pathotype -q` |
| `research_outputs/pathotype_model_state_and_bakeoff_plan_2026-05-31.md` | the confound + bake-off analysis |

---

## 7. Project north star (unchanged)
"AI DNA decoder TOOL, not papers; failure-tolerant iteration; honest scope limits." The honest
0.833 cap and the "stop tuning a confounded substrate" call ARE the north star in action — the
frontier is **labels, not models**, and the project has now demonstrated that three times.
