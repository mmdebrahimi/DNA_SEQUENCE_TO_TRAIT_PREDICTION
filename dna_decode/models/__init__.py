"""Foundation-model wrappers + embedding cache + downstream classifiers.

Modules (populated by later plan steps):
- foundation: Step 7 — Evo + DNABERT-2 + Nucleotide Transformer + GENA-LM wrappers
- cache: Step 8 — HDF5-backed embedding cache
- classifiers: Step 9 — frozen-embedding XGBoost task heads
"""
