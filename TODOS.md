# TODOs

Deferred work and post-Phase-1 polish.

See also: `plans/Ecoli_G2P_Phase1_Ship_Path_Plan.md` for the contracted ship-path plan (archived 2026-05-12); `FUTURE_FEATURES.md` for Phase 2+ ideas.

## Phase 2 — Entry criteria (gate before any Phase 2 work)

- [ ] Real-data smoke: one E. coli genome end-to-end with prediction + attribution captured (no ground-truth comparison required at entry)
- [ ] Full test suite passes on real deps (`uv run pytest tests/ -v` — 287 tests as of 2026-05-12)

## Phase 2 — Real-data validation (deferred from Phase 1)

- [ ] Real-data pilot gate against a downloaded BV-BRC AST TSV — confirm ≥150 strains per drug for cipro / ceftriaxone / tet after broth-microdilution + assembly-quality filters.
- [ ] End-to-end `pipeline.py ingest → train → attribute` on the pilot-validated cohort; record CV AUROC + Tier 1-3 fractions per drug.
- [x] Mash CLI install on the target machine — RESOLVED 2026-05-15 via Docker route (`quay.io/biocontainers/mash:2.3--hb105d93_10` pulled + smoke-validated on 2 cipro-R strains; cross-distance 0.0324, self-distance 0; see `wiki/stage2_install_artifact_2026-05-15.md`). `tools/docker_runner.py` invokes; `compute_mash_distances(..., use_docker=True)` routes through it. Native binary on PATH also still works (Linux/WSL2 `apt install mash`).
- [x] GPU constraint observed 2026-05-14: actual hardware is GTX 860M (4 GiB Maxwell, CC=5.0, 2014). 4-bit Evo via bitsandbytes requires CC ≥ 7.0 — structurally unavailable. DNABERT-2 blocked by Triton 3.x state_dict incompat. NT v2 100M works locally (slow, ~9 min/strain). Larger-cohort runs (≥150 strains) gated on Databricks burst.

## Phase 2 — Starter genome set (infrastructure smoke; NOT a model-quality test)

For the one-genome / few-genome end-to-end smoke that gates Phase 2 entry. Useful for proving the ingestion → annotation → embedding → cache → train → attribute pipeline runs against real NCBI data. **Not** suitable as a model-quality test — cross-lineage R/S contrast confounds clade signature with resistance signal, which is exactly the trap the clade-only-baseline gate is supposed to detect.

| Strain | Assembly accession | Role |
|---|---|---|
| K-12 MG1655 | `GCF_000005845.2` | Reference baseline (non-pathogenic, well-annotated) |
| O157:H7 Sakai | `GCF_000008865.2` (verify via NCBI Datasets) | Pathogenic comparison |
| ST131 EC958 | `GCF_000285655.3` (verify via NCBI Datasets) | Multidrug-resistant clinical lineage |

Download via:
```bash
datasets download genome accession GCF_000005845.2
```

- [ ] Phase 2 real model-quality test needs **within-lineage** susceptible+resistant pairs (or clade-stratified cohort), NOT the cross-lineage trio above. Use the trio for infrastructure smoke only.

## Phase 2.5 perf hardening (deferred from Gate B prep)

- [ ] **`load_bvbrc_ast` is slow on real BV-BRC AMR exports** — Python pandas engine (sep=None) + iterrows() over 50K+ rows takes minutes. Audit cohort generator skips --ast on real data because of this. Fix: peek at first line to detect separator → use C engine; replace iterrows with vectorized ops. Surfaced 2026-05-13 during audit_cohort.py run.
- [x] **Embedding cache populate is per-sequence on GPU** — RESOLVED 2026-05-13. Added `_embed_window_batch` to `FoundationModel` base + override in `NucleotideTransformerModel` with mask-aware mean pooling. `cache.populate()` now chunks pending pairs into `EMBED_BATCH_SIZE=4` groups (tuned for 4 GiB VRAM on GTX 860M; raise to 32+ on larger GPUs). Regression test `test_nt_embed_window_batch_matches_per_sequence` confirms numerical equivalence with per-sequence path within rtol=1e-4. Empirical speedup on GTX 860M Maxwell was modest (~20%) — kernel-launch overhead is small fraction of compute-bound forward pass on this GPU; speedup would be 5-25× on Ada/Ampere hardware.
- [ ] **[BLOCKED] NT AutoModel swap** (`foundation.py:239`) — `AutoModel.from_pretrained` fails at load on the NT v2 100M `trust_remote_code` checkpoint with state_dict shape mismatch (`Linear[4096, 512]` vs `Linear[512, 2048]`). InstaDeep's `trust_remote_code` defines architecturally distinct AutoModel vs AutoModelForMaskedLM variants. Deferred indefinitely per `plans/Audit_Calibration_NT_AutoModel_Ship_Path_Plan.md` Commit 2 gating rule.
  - Environment at failure: `transformers==5.8.0`, `torch==2.11.0`, `python==3.11.5`, os=`Windows 10`, cuda=`available`, NT revision=`f34324c6fde36a4f635f0f1f06cac5d25acd6798` (HF cache at `F:\hf_cache\hub\` via `HF_HOME` env var), loader=`AutoModel.from_pretrained(trust_remote_code=True)`.
  - Revisit when: InstaDeep ships an AutoModel-compatible checkpoint, OR project drops `trust_remote_code` in favor of a manual reimplementation.
- [ ] **[OPEN] NT hidden-state hardening** (`foundation.py:259`) — current code calls `model(**inputs, output_hidden_states=True)` then takes only `hidden_states[-1]`. Drop intermediate-layer materialization for a low-risk speedup. AutoModel swap is blocked (see above); diagnostic spike needed to check whether the loaded `AutoModelForMaskedLM` exposes a base-encoder accessor (`.base_model` / `.encoder` / `.nucleotide_transformer`) returning final hidden state directly. Defer until NT becomes critical-path during Phase 2 smoke.
- [ ] **[OPEN] Cache-integrity validation before `.complete` sentinel** (`refseq.py:_unpack_ncbi_datasets_zip` + `download_genome`) — current code writes extracted files non-atomically then drops `.complete` sentinel. `is_cache_complete()` checks only sentinel presence, not file content. Surfaced 2026-05-13: `GCA_003204155.1/genome.fna` had a 4,100-byte block of binary garbage starting at byte 1,159,168 (likely partial overwrite from a prior interrupted run); `.complete` was still present so subsequent `download_cohort_genomes` calls skipped it for weeks. Suggested fix BEFORE `.complete` is written: (a) `SeqIO.parse(fasta)` smoke-parse succeeds, (b) sequence alphabet ⊆ `{A,C,G,T,N}`, (c) file size within plausible range for the organism, (d) cross-check extracted `genome.fna` bytes against the genomic `.fna` member inside the cached `package.zip`. Recovered manually for the 12-strain mini-cohort run; permanent fix deferred.

## Phase 2 — Decision gate prerequisites and deferred classifier variants

Per `plans/Phase2_Decision_Gate_Plan.md` (saved 2026-05-13). Tiered N=50→N=150 staged path with Option-C threshold (≥5 pp AUROC delta AND top-K attribution includes gyrA / parC / parE).

- [ ] **[OPEN] Real clade-only baseline (Stage 1 prerequisite, NOT smoke gate)** (`scripts/pipeline.py:285`, `dna_decode/eval/clade_baseline.py`, `dna_decode/data/cohort.py`) — originally framed as smoke-gate prerequisite; **rescoped 2026-05-14** to Stage 1 N=50+ per B-B decision lock (smoke gate drops clade-only entirely; 12 unique MLST → singleton clades → AUROC ≈ 0.5 regardless of fix). Stage 1 scope:
  - **Replace `hash(s.mlst) % 10` placeholder** with `mlst_to_clade_id` helper in `dna_decode/data/cohort.py` (deterministic via `zlib.crc32`, scheme-aware; see `plans/Sidework_Sequence_Ship_Path_Plan.md` D7 pseudocode).
  - **Auto-disable with warning on sparse cohorts.** Empirically verified 2026-05-14 on `gate_b_cohort.parquet`: **61 unique MLSTs across 67 strains** (unique_mlst_fraction=0.910). At Stage 1 N=50 sampled from this, sparsity will remain ≥0.85 — clade-only stays degenerate even after the parsing fix. Pattern: `if unique_mlst_fraction > 0.8: logger.warning("Clade-only baseline disabled: insufficient lineage reuse (fraction={f:.2f} > 0.8)"); skip_clade_baseline = True`.
  - **Coarser binning escape hatch.** ST-level grouping is too fine for the cohort's MLST diversity; consider binning to (a) scheme name only (`ecoli_achtman_4` vs `Escherichia_coli_1` — only 2-3 groups), OR (b) Mash clusters at ANI≥99% (true lineage grouping). Without coarser binning, clade-only stays degenerate at any cohort size up to gate_b's 67-strain limit.
  - **Strain-keying semantics fix** in `predict_clade_only` / `per_clade_baseline` — current code assigns same clade AUROC to every strain in a clade group regardless of LOSO-fold structure. Separable concern flagged by /review 2026-05-13.
- [ ] **[OPEN] Add Random Forest wrapper to `classifiers.py`** — for Stage 2 decision gate (N=150). Field-standard downstream classifier on frozen DNA-FM embeddings per the 2025 Nature Communications benchmark (research_outputs/sota-bacterial-amr-prediction-small-cohorts-2026-05-13.md). Straightforward sklearn-style wrapper alongside existing XGBoost path.
- [ ] **[OPEN] Add TabPFN wrapper to `classifiers.py` with PCA-to-≤2000-feature step; pin package version** — for Stage 2 decision gate. NT outputs 512-dim → fits TabPFN-2.6's 2000-feature envelope directly, but PCA still recommended for stability + reproducibility (and to fit older TabPFN-2's 500-feature limit if package falls back). McElfresh NeurIPS 2024 shows TabPFN beats XGBoost on ≤1250-sample regime (rank 4.88 vs 8.30). Pin exact TabPFN version for reproducibility.
- [ ] **[OPEN] Add SNP-table feature variant — parse AMRFinderPlus output (`--organism Escherichia --mutation_all`) for POINT* rows** — for Stage 2 decision gate. AMRFinderPlus already emits point mutations as `gyrA_S83I` / `parC_S80I`-style rows (Method column = `POINT`, `POINTP`, `POINTX`, or `POINTN`) when run with the right flags. Treat as binary features alongside existing gene-presence. Cipro-specific classical baseline: gyrA / parC / parE rows are the textbook resistance signal. Scope: hours (parsing existing AMRFinder output), not days (no variant-calling pipeline needed).
- [x] **XGBoost calibration overcorrection at N≤20 training** (`dna_decode/models/classifiers.py:train_xgboost_classifier(..., calibrate=True)`) — RESOLVED 2026-05-14 via Stage 1 refactor. Stage 1 + smoke gate now pass `calibrate=False` at all 3 gate-bearing call sites (NT-XGBoost, NT-logreg, k-mer-XGB); `_train_baseline_logreg` in `classical_baselines.py` gained a matching `calibrate: bool = True` kwarg that returns raw `LogisticRegression` without the `CalibratedClassifierCV` wrapper when False. Pinned by 3 calibration-discipline tests. Long-run decision (re-enable at N=50 vs guard at N≥30) deferred until Stage 2 N=150 cohort lands; at that scale calibration CV has enough samples per fold and the question becomes empirical.
- [x] **Gene-presence + XGBoost AUROC=0.000 root cause resolved 2026-05-14.** Two compounded mechanisms: (1) `parse_gff3` was extracting strain-unique GFF3 `ID=gene-b0001` identifiers into `gene_id`, so cross-strain vocab overlap was 0.00%; (2) at 0% overlap, every test row is all zeros and XGBoost predicts the training class prior, which under LOSO on a balanced cohort always opposes the held-out label → perfect rank inversion. Fix: added `gene_symbol` column to `AnnotationTable` (populated from GFF3 `gene=` attribute) + `_extract_gene_ids` now prefers `gene_symbol`. Added `INDETERMINATE_IDENTIFIER_OOV` guardrail in smoke runner (median overlap < 20% → mark indeterminate rather than report misleading AUROC). Post-fix overlap = 10.84% — still below threshold because RefSeq GFF3 only carries `gene=` for ~11% of CDSs. Result correctly marked INDETERMINATE on this cohort. NT-XGBoost (0.750) + k-mer (0.694) unaffected; smoke verdict still PASS. See `plans/Gene_Presence_AUROC_Bug_Fix_Plan.md` + commit log.
- [ ] **[OPEN] Gene-presence vocab coverage is data-limited for RefSeq GFF3** — surfaced 2026-05-14 during the AUROC=0.000 fix. After the `gene_symbol` fix, median cross-strain vocab overlap is only 10.84% because RefSeq GFF3 carries `gene=` for ~11% of CDSs (the rest are hypothetical / unannotated proteins with only strain-unique `ID=gene-bXXXX`). Options for Stage 1+: (a) **Bakta re-annotation** — Bakta typically calls more gene symbols + uses cross-strain stable locus tag schemes; (b) **Roary / OrthoFinder pan-genome clustering** — assigns orthogroup IDs that DO generalize across strains; (c) **Stick with RefSeq + accept gene-presence is structurally uninformative at small N for this annotation source** — gate gene-presence to INDETERMINATE in all cohorts using RefSeq GFF3 alone. Decision needed before Stage 1 N=50 if gene-presence is intended as a comparator.
- [ ] **[OPEN] Lineage-leakage check on the 12-strain NT smoke result** — surfaced 2026-05-14 via /brainstorm. The smoke gate used LOSO on the 12-strain cohort where 11 of 12 strains have unique MLSTs (one strain has a singleton; MLST diversity is structural). LOSO at this MLST diversity doesn't actually control for lineage signal — held-out strains land in clades unseen during training, so any classifier that learns lineage memorization scores well via "this is a novel lineage" embedding distance, not biology. Decision: before treating the +5.6 pp NT lift as "real signal," overlay MLST scheme + ST onto the embedding PCA/UMAP plot (the planned diagnostic step) AND check whether NT-XGBoost's per-strain probability tracks resistance OR lineage. If lineage tracks better than resistance: the smoke result is confounded. Not blocking Stage 1 entry; informs Stage 2 design (Mash-clustering-stratified CV may be required).
- [x] **Stage 1 quantitative success criterion locked 2026-05-14: (b) ≥3 pp AUROC lift over best classical baseline under LOSO at N=40.** Acknowledged: this is inside the ±0.10 noise floor at N=40 — Stage 2 (N=150 Databricks burst) tightens to the (c) Option-C threshold (≥5 pp + biology check via gyrA/parC/parE attribution). Stage 1's role is engineering screen + go/no-go for spending N=150 burst budget, not final ship gate.
- [x] **Stage 1 runner + 4-experiment matrix shipped 2026-05-14** (`scripts/stage1_n40_cipro.py`, 11 unit tests at `tests/test_stage1_n40_cipro.py`). Matrix: NT-XGBoost (primary gate) + NT-logreg (sanity check; flags plumbing-bug if it beats XGBoost per H13) + k-mer-XGB (classical comparator) + NT+k-mer-fusion-logreg (diagnostic only, NOT gate-bearing). Gate formula: `max(NT-XGBoost, NT-logreg) − k-mer-XGB ≥ 3 pp`. All variants use `calibrate=False`. Paired bootstrap CI (B=1000) on the gap. Verdict labels: ≥5 pp CLEAN / 3-5 pp NOISY / <3 pp FAIL. Per-MLST breakdown + per-strain LOSO predictions table in result packet. Gated on N=40 NT populate completion (running 2026-05-14 PM).
- [x] **Stage 1 runner refactor + test hardening shipped 2026-05-14** (`scripts/stage1_n40_cipro.py`, `dna_decode/eval/loso_kmer.py`, `dna_decode/eval/cv.py`, `dna_decode/models/classical_baselines.py`, +13 tests at `tests/test_stage1_n40_cipro.py`). Stage 1 runner is now thin orchestration over `cv.py` (NT variants via `leave_one_strain_out_cv`) + new `loso_kmer.py` module (k-mer + fusion runners shared with smoke gate). New: `CVResult.strain_ids` alignment-contract property; `CONTIG_SEPARATOR` module constant; `_train_baseline_logreg(..., calibrate=False)` calibration-skip branch. Verdict semantics frozen as pure function of point gap; `stage2_action` is a separate decision-layer field (`BURST_STAGE_2` / `HOLD_STAGE_2_CI_DEGENERATE` / `ALTERNATIVE_POOLING_RERUN` / `PIVOT_TO_BAKTA`) per the plan's Verdict-Time Pre-Commitments table. `compute_gate_outcome` validates `strain_ids` alignment (NT-vs-k-mer mismatch raises; fusion mismatch suppresses fusion-outperforms note diagnostic-only). Exit 0 only when `stage2_action == BURST_STAGE_2`. 399 tests passing (was 386 baseline; +13 covering fusion-exclusion, calibration discipline at all 3 gate-bearing sites, stage2_action mapping, MLST loud handling, alignment validation).
- [x] **Stage 2 prep plan + cohort builder + supporting infrastructure landed 2026-05-14 PM.** Plan at `plans/Stage2_N150_Prep_Plan.md` (4 phases A-D; resolves Bakta vs Roary vs accept-degenerate, AMRFinderPlus integration scope, Databricks vs local compute, cohort source). Cohort built at `data/processed/stage2_n150_cipro_cohort.parquet` (147 strains, 72R/75S — 3-strain shortfall vs target 150 is the natural BV-BRC ceiling at N50≥50K + contig_count≤500 + MLST-present + assembly_accession filters). Builder: `scripts/build_stage2_n150_cohort.py` with label-stratified MLST-balanced selection (per-class). Diagnostic: `scripts/diagnose_bvbrc_mlst_gaps.py` (layer-by-layer cohort-shortfall profiler). Defensive patches: `parse_gff3` parent→CDS gene_symbol propagation (`annotations.py`, 3 tests) for Bakta-style GFF3; `leave_one_*_out_cv` loud-fail on unassigned strains (`cv.py`, 2 tests) for Stage 2 Mash-clade-out CV. `VariantResult` internal length-consistency invariant in `compute_gate_outcome` (2 tests). Suite: 420 passing (was 399 + 14 epilogue + 7 from this stretch). Phase A.1/A.2/A.5 installs (Bakta + AMRFinderPlus + Mash) pending user-side Docker Desktop startup.
- [x] **HDF5 populate checkpoint-flush per strain shipped 2026-05-14 PM** (`dna_decode/models/cache.py:populate`, commit f9ed79f). After each strain's chunked-write batch, `f.flush()` pushes the HDF5 superblock + EOA marker forward to disk. Failure-mode cost: ~last-strain recompute (~10-15 min) instead of full-populate loss (~8 hr). Pinned by `test_populate_flushes_hdf5_per_strain_for_usb_hiccup_resilience` (monkeypatched h5py.File spies on flush calls; asserts >=N strains → >=N flushes).
- [x] **Mash pairwise distance refactor** (`dna_decode/eval/phylogeny.py:compute_mash_distances`) — SHIPPED 2026-05-15 (commit 77b504b). Single `mash sketch -o sketch.msh <all-fastas>` + single `mash dist sketch.msh sketch.msh`. 2 invocations vs prior N*(N-1)/2 (10,731 at N=147 → 2). New `use_docker=True` kwarg routes through `tools/docker_runner.run` (pinned image `quay.io/biocontainers/mash:2.3--hb105d93_10`). 19 regression tests (was 13) including batched-call-count pin proving N→2 regardless of cohort size.
- [x] **Bakta install + DB landed 2026-05-15.** `oschwengers/bakta:v1.11.4` pulled + DB v6.0 light extracted to `C:/Users/Farshad/dna_decode_stage2/bakta_db/db-light/` (4.0 GB; Zenodo DOI 10.5281/zenodo.14916843; MD5 verified). Workaround documented: `bakta_db download` AMRFinder dep-check fails when invoked via `--entrypoint /opt/conda/bin/bakta_db` (skips conda activation) — wrap in `--entrypoint /bin/bash -c "bakta_db ..."`. K-12 + cipro-R annotation smoke deferred to post-Stage-1 (CPU-heavy ~5-30 min/strain).
- [x] **AMRFinderPlus integration landed 2026-05-15** (install + smoke). `ncbi/amr:4.2.7-2026-03-24.1` pulled; DB at `C:/Users/Farshad/dna_decode_stage2/amrfinder_db/latest/` (240 MB, version `2026-03-24.1`). Smoke verified on ST131 cipro-R substrate `1328433.3 / GCA_000522345.1`: `mutations.tsv` 668 rows; **textbook QRDR detected** (`gyrA_S83L` + `gyrA_D87N` + `parC_E84V` — all QUINOLONE-class POINT rows). Codex-flagged invocation pattern confirmed: `amrfinder -n <fna> -O Escherichia --database /db/latest --mutation_all /out/mutations.tsv -o /out/main.tsv`. Parser-into-smoke-runner integration tracked separately under EP2 / EP3 design.
- [x] **EP2 cef + tet smoke runner generalization** — PARTIALLY DONE 2026-05-17. Output strings templated on `--drug` shipped (`scripts/smoke_gate_12strain_cipro.py` lines 264, 272, 341). NT-XGBoost runner falls back to `ast_labels` iteration when `cohort.per_drug_strain_ids[drug]` missing — lets mini cohorts built outside `build_cohort()` drive the smoke runner. Both cef + tet smokes fired 2026-05-17 (cef PASS 0.833 = 0.833; tet FAIL anti-predictive 0.400 vs 0.722 k-mer; H17 falsified). Mechanism-aware baselines (AMRFinder POINT + Bakta gene-presence wiring per source plan Step 3) REJECTED via /review + /brainstorm rounds 1-3 — wrong instrument for the lineage-vs-mechanism question at N=12 ±0.19 CI. Reduced to a cheap NT-vs-mechanism cross-tab diagnostic in `plans/Cef_Mechanism_Audit_Plan.md`. Rename `smoke_gate_12strain_cipro.py` → `smoke_gate_12strain.py` still pending (tech debt; see below).
- [ ] **[OPEN] EP3 attribution audit-runner build** — surfaced 2026-05-15 by Codex post-reset implementability review. `dna_decode/interp/mutagenesis.py` has primitives (`gene_level_mutagenesis`, `AttributionReport.fraction_fail`, `build_attribution_report`) + `scripts/pipeline.py attribute` emits one-strain output, but no cohort-level audit runner. Cipro version SHIPPED 2026-05-16 as `scripts/cipro_attribution_preflight.py` (v2 expanded loci + signed-positive-delta + frequency aggregation; INCONCLUSIVE_MISS on cipro mean-pool). Cef + tet versions deferred to Phase 2 if attribution becomes critical. Required for cef attribution port: replace `CIPRO_LOCI_BY_MECHANISM` with `CEF_LOCI_BY_MECHANISM`; same gene-level ISM logic; ~80% reusable.
- [ ] **[OPEN] `/project-state` candidate-actions refresh feature gap** — surfaced 2026-05-15. v0.3 `--refresh-frame` updates only current-state + target-state; the Bellman frame's `### Candidate next actions` table has its own end-marker but no skill flag to refresh it via the byte-preserving anchor-based mutation pattern. Falls back to direct Edit. File as v0.4 feature request.
- [ ] **[OPEN] `scripts/stage1_n40_cipro.py` exit-code-vs-stage2_action mismatch** — surfaced 2026-05-15 13:12 EDT after Stage 1 verdict landed. CLAUDE.md gotcha says "Exit 0 ONLY when stage2_action == BURST_STAGE_2"; observed exit code 0 with stage2_action == ALTERNATIVE_POOLING_RERUN (verdict FAIL). Either runner bug (should have exited 1 per spec) OR CLAUDE.md doc drift. Trace `compute_gate_outcome` → exit-code mapping; verify against `plans/Stage1_N40_Cipro_Engineering_Screen_Plan.md` Verdict-Time Pre-Commitments table.
- [x] **Stage 1b ALTERNATIVE_POOLING_RERUN design + execution** — SHIPPED 2026-05-16. `aggregate_strain_features` already supported "mean+max" (1024-dim concatenation). Refactored `stage1_n40_cipro.py` to take `--aggregation` flag (default `mean+max`). Stage 1b verdict packet at `wiki/stage1_n40_cipro_mean-plus-max_2026-05-16.md`: NT-LR 0.673 vs k-mer 0.648 (gap +2.5 pp, still BELOW 3 pp threshold; CI [-20, +23]). NT-XGB 0.615 (recovered from 0.568 with mean-pool). Verdict FAIL but per-variant recovery confirms pooling matters. PIVOT TRIGGER condition 4 (curated AMR baseline) remained untested; Stage 1b closed.
- [x] **H13 follow-up: NT-logreg AUROC 0.100 anti-predictive at Stage 1 N=38** — RESOLVED 2026-05-15 → 2026-05-16. Root cause: feature-scaling, NOT row-order / label / class-mapping. NT embeddings are 512-dim dense floats with arbitrary per-dim scale; unscaled LR with default L2 regularization overwhelmed by feature magnitudes + lineage-tracked. Fix: `_train_baseline_logreg(..., scale_features=True)` wraps LR in `sklearn Pipeline(StandardScaler, LR)`. K-mer + gene-presence (binary 0/1 features) intentionally do NOT scale. Stage 1b 2026-05-16 with scaled-LR recovered NT-LR from 0.100 → 0.673 (57-pp recovery). Pinned by test at all 3 gate-bearing call sites.

## Phase 1 closeout follow-ups (deferred audit debt; 2026-05-17)

Surfaced during the EP1 closeout + /brainstorm framing critique. ALL items are appendix-tier maintenance, NOT Phase 1 reopen triggers per the narrowed stopping rule.

- [ ] **[OPEN, appendix-tier]** Cef NT-vs-mechanism cross-tab (`plans/Cef_Mechanism_Audit_Plan.md` Step 3). Cheap diagnostic (~1 hr CPU): per-strain join of `(true label × NT-XGBoost prediction × AMRFinder β-lactamase hit)`. Corroborates cef PASS at 0.833 but does NOT change the EP1+EP2 synthesis. May or may not fire; synthesis is defensible without it.
- [ ] **[OPEN, deferred]** Mean+max attribution preflight v3 (per `plans/Cipro_Decision_Bundle_Technical_Plan.md` Step 2 + Step 4). Refactor: add `aggregation` kwarg to `gene_level_mutagenesis` at `dna_decode/interp/mutagenesis.py:124,142`; add `NotImplementedError` shield to `saturation_mutagenesis` for non-mean. ~2 hr coding + ~10 min runtime. Closes the cipro attribution audit trail by testing whether mean+max pooling recovers QRDR loci in top-K=20 (v1/v2 mean-pool both missed). Deferred per /brainstorm round 3 framing critique — closeout falsifier, not Phase 2 entry-grade.
- [ ] **[OPEN, deferred]** Smoke runner silent-variant-drop bug fix (`plans/Cef_Mechanism_Audit_Plan.md` Step 1). `scripts/smoke_gate_12strain_cipro.py:374-382` had `try/except Exception` swallowing variant failures silently; today's cef + tet smoke reports both missing gene-presence row. Fix: re-raise on unexpected errors; render `INDETERMINATE_<reason>` rows; emit per-strain predictions JSON sidecar. ~1 hr. Picks up when convenient.
- [ ] **[OPEN, deferred]** `smoke_gate_12strain_cipro.py` → `smoke_gate_12strain.py` rename (existing tech debt; EP2 source plan Step 2). Output strings templated on `--drug` 2026-05-17; only the filename + script header remain cipro-named. Cosmetic; not blocking any downstream work.
- [ ] **[OPEN, deferred]** Bakta cohort-wide annotation (~7.6 hr CPU; toolchain validated 2026-05-15 but never run cohort-wide). Required for any future Phase 2 gene-presence-feature experiment that wants Bakta annotations over RefSeq. Deferred indefinitely; not on Phase 2 critical path.
- [ ] **[OPEN, deferred]** Cipro Stage 1 pure architectural verdict isolated from cohort noise. The N=38 Stage 1 FAIL was confounded by cohort label-noise (per EP1 closeout merge audit). Pure architectural test would need either a clean N=40 cipro cohort with strict-MIC labels OR a re-run on the 7 CLEAN_R + decisive S subset (degenerate at N=8). Deferred per EP1 closeout PC1=`internal_closeout` discipline; resurrected only under Phase 2 if cohort expansion is feasible (gated on BV-BRC strict-MIC feasibility census).
- [ ] **[OPEN, deferred]** Tet failure-mode disambiguation. Tet 0.400 anti-predictive could be (a) architectural mismatch on distributed mobile-element signal (most likely per cross-drug pattern), (b) tet-cohort label-noise carryover (not audited), or (c) N=12-calibration interaction (less likely). Cheap re-fire option: scaled-LR variant on tet mini-cohort (~30 min). Full tet mechanism × MIC audit option: ~1-2 days. Both deferred; current evidence supports the architectural-mismatch story without further disambiguation.

## Phase 2 entry (deferred to fresh session per stopping rule)

- [ ] **[OPEN, Phase 2 entry]** BV-BRC strict-MIC 3-drug feasibility census (cef + tet + 4th-drug candidate like colistin mcr-family OR aminoglycoside aac/aph/aad). Cheapest experiment that materially changes Phase 2 strategy: gates whether Phase 2 axis is cohort expansion vs architecture repair vs 4th-mechanism-class smoke. Pandas-only (~30 min code + 5-10 min runtime). Per `/brainstorm` 2026-05-17 verdict + `project_state/dna-decode-2026-05-11.md` Candidate-next-actions row 1. **Open Phase 2 with `/idea-anchor` + `/project-init` first** per the synthesis's stopping rule before firing this census.

## Pre-existing known limitations (not bugs)

- **Live BV-BRC API integration**: `pilot.fetch_bvbrc_drug_counts` raises `NotImplementedError` when no `--ast-tsv` flag / env var / config entry is provided. Live REST endpoint resolution deferred until first real-data run. Workaround: download an AST TSV/CSV from BV-BRC.
- **`fetch_ncbi_assembly_quality`** in `pilot.py` stays scaffolded intentionally. Phase 2 ships a separate CSV adapter (`dna_decode/data/bvbrc_genome.py`) that bypasses it via `pipeline ingest --assembly-metadata-csv`. Live NCBI Datasets REST integration is Phase 3 work.
- **Annotation source variance**: `parse_gff3` collapses `ID=` / `Name=` / `gene=` into one `gene_id` column. Annotation-source-aware extraction (gene_symbol as separate column) is Phase 2 cleanup work.
