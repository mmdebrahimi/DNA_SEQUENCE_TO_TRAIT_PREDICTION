<!-- unsupported-schema: 0.4 -->
# Unsupported / rejected rows — free-genotype-paired-phenotype-substrates-2026-07-16

0 rows rejected. All 10 candidate rows carried a verbatim quote + value + real https:// source and passed the
audit floor + banned-phrase + cite-token scans. Confidence classification: 2 `high` (direct WebFetch of the
PGS Catalog About page), 7 `medium` (WebSearch-summary provenance → downgraded from any high per the search-
snippet rule), 1 `low` (Y1000+, directional only). See the raw memo's "Honest gaps" for documented absences.
