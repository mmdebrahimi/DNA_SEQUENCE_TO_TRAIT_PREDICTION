# Soraya --advance run — intent contract (immutable)

- **Run:** 2026-06-22-adv1-hiv-ols-baseline
- **Mode:** --advance (assess + move forward, best judgment)
- **Directive:** "assess the situation and move forward according to your best judgment"

## Situation assessment
- Single-ledger project (no umbrella/families). Authoritative Bellman current-state (2026-06-19):
  AMR track BANKED + frozen; genome-map honesty tool GO + productized; embedding/MIC tracks closed
  negatives; the two big forward paths (label acquisition / prospective-lock) are USER strategic
  decisions, NOT executor tasks. The 2026-05-17 candidate-actions table is stale (Phase-2-entry).
- Executor-eligible, free-data, non-blocked backlog is thin (TB cell is data-run-blocked; genome-map
  scale-up is a user investment call).

## Selected highest-VOI move (best judgment)
Close the validation-rigor gap I OPENED last turn: the PI/INSTI/CAI HIV cells shipped with only the
catalog-vs-PhenoSense AUC (tool-works half) but NO OLS underlying-tool baseline (does-the-layer-add-value
half) — the project's load-bearing wrapper-vs-tool discipline. The HIV report card's OLS columns are
empty for those 3 classes. Closing it produces a genuine new conclusion (does the deterministic
position/mutant catalog tie/beat the OLS regression for PI/INSTI/CAI, as it did for NNRTI?) and finishes
the job to the project's own standard. NOT gilding (visible incompleteness), NOT plateau (new info).

## Batch (planned, all `auto` per action_gate)
1. Generic OLS baseline scripts/hiv_targetsite_baseline.py (reuse hiv_nnrti_baseline machinery).
2. Run for PI/INSTI/CAI -> wiki/hiv_{pi,insti,cai}_baseline_vs_ols_<date>.{md,json}.
3. Verify-in-batch: inspect the catalog-vs-OLS deltas (decide code-vs-expectation).
4. Wire deltas into build_hiv_report_card.py (populate the empty OLS columns).
5. Tests + full suite + frozen-surface check.
6. Commit + ledger row 149 + recommendation/result.

## Gates
All steps classify `auto` (local code/edits/tests; datasets in hand, gitignored). No money, no
destructive-local, no irreversible. Attended single run.
