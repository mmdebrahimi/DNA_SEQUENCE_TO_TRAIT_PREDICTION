# recommendation — 2026-06-22-adv1-hiv-ols-baseline

The executor-eligible, free-data, non-blocked backlog is now ~exhausted. The HIV cell is complete to the
project's full validation standard (catalog AUC + OLS baseline + per-class). The remaining moves require a
USER decision (not an executor task), per the 2026-06-19 Bellman current-state:

1. **Label ACQUISITION** (non-public wet-lab/clinical) — clears the label gates by construction; anchor at
   `wiki/next_epoch_idea_anchor_prompt_2026-06-13.md`. The single highest-leverage move (the binding
   constraint is labels, not models).
2. **PROSPECTIVE-LOCK validation** of the frozen decoder against later-arriving independent data.
3. **Genome-map scale-up** (catalog integration at scale; richer homology/Pfam tiers) — a user investment call.
4. **TB AMR data runs** — code shipped (organism_rules/), BLOCKED on ~1.6 TB regeno -> D: cache + a
   hand-curated post-2023 gold set (external/resource wall, not code).

Lower-value executor leftovers (NOT recommended now — diminishing returns): HIV per-drug differential
resistance (the OLS coefficients above already bound this), HIV v0.1 mutant-specific PI/INSTI catalogs.

**Recommendation: bank here and pick a forward path (1-4) as a user decision.** Continuing to deepen HIV
would be motion, not signal.
