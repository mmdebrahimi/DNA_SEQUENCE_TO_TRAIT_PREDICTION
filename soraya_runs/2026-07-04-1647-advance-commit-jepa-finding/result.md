# Soraya --advance — result (2026-07-04-1647)

## Intent
`/soraya --advance` on the dna_decode umbrella. Pick the highest-VOI eligible family, execute a bounded
reversible batch, spillover, stop at a genuine terminal.

## Eligibility scan (code-owned candidate-next-actions, refreshed 2026-07-02)
| # | Candidate | Verdict |
|---|---|---|
| 1 | TB independent gold-set (Thorpe 2024) | **STALE + EXTERNAL WALL** — the "first independent TB number" milestone this candidate targets ALREADY LANDED via the EBI AMR-Portal cohort (RIF 0.937/INH 0.914 raw; lineage-collapsed 2026-07-02). A Thorpe gold-set is now only a SECOND confirmation (lower value); still network+Docker+underpowered. Not a bounded reversible batch. |
| 2 | Acquire non-public measured-label source | **USER-AUTHORITY WALL** — advanced THIS session (UK Biobank chosen path, application drafted). Blocked on the user (register + MTA signatory + fee). |
| 3 | Prospective-lock accrual | **EXTERNAL WALL** — data must accrue post-2026-06-13 + manual network step. |
| 4 | genome-map v1 deferred features | fresh feature-design cycle + D: cache; NOT a bounded advance batch. |
| 5 | AMR-Portal remaining cells | **PLATEAU / recorded stop** — class-C/D not gene-presence-decodable. |

**PLUS an off-table reversible-value gap found in the working tree:** this session's tested, validated
learned-representation finding (JEPA/DMS) was UNTRACKED (at risk of loss).

## Batch executed (1 genuine high-VOI reversible move)
1. **Verified** the untracked work is done, not a dead-end: `wiki/jepa_dms_learned_signal_result_2026-07-04.md`
   is a PASS (AlphaMissense vs ProteinGym DMS median |Spearman| 0.417 vs 0.029 shuffled; field ~0.48).
2. **Ran the tests** (verify-in-batch): DMS falsifier 5/5 (2s) + JEPA prototype 6/6 torch (402s). 11/11 green.
3. **Confirmed** frozen AMR surface untouched (additive only); uv.lock change = legitimate 0.6.4→0.6.5 sync.
4. **Committed + pushed** the bundle (2 scripts + 2 tests + 3 J-family plans + result memo + 10 prior
   soraya_runs audit records + uv.lock sync + .gitignore crash-dump). Commit `09c098d` (rollback:
   `git revert 09c098d` — purely additive).

## Terminal
**GENUINE PLATEAU (re-aimed once, executed the real move, now banking).** After committing the validated
finding, every remaining candidate is a hard-gated wall:
- **user-authority** (UK Biobank acquisition — user registers/signs/pays),
- **external/GPU** (J2 real-sequence-representation train = GPU/Databricks; TB gold-set = network+Docker;
  prospective-lock = data accrual),
- **recorded plateau** (AMR class-C/D ABSTAIN).

No bounded reversible local batch of real value remains. Not stopping on a first increment — the one real
reversible move (durably banking the at-risk JEPA/DMS finding) was taken; the next real moves are all gates.

## Wall classification
- **UK Biobank** → EXTERNAL (user authority + money; converts to a code wall the instant access lands →
  then the UKB-RAP ingestion scaffold becomes a real bounded batch).
- **J2 JEPA train** → EXTERNAL (GPU — GTX 860M can't; Databricks burst). The falsifier + plan are ready;
  the compute is the wall.
- **TB gold-set / prospective-lock** → EXTERNAL (network+Docker; data accrual).
- **AMR class-C/D** → recorded terminal (needs expression/regulatory signal, not another gene-presence rule).

## No-resume
This run is complete. Re-invoke `/soraya --advance` after a gate clears (e.g., UKB access lands, or a GPU
host is available for J2).
