<!-- intent-contract: 1.0 -->
# Soraya --until-mvp — tb-goldset (help the user build the independent TB golden baseline) + indel-norm
- Directive: "help me create the golden baseline (I'm a SWE, not a TB curator); move forward with the other recs (indel-norm)"
- MVP bar (execute-mode draft-then-ratify):
  (a) GOLD-SET HELP: a playbook + the provably-not-in-CRyPTIC leakage check + an ingester that turns a
      source TSV into the scored manifest the existing arm consumes. (the scored INDEPENDENT number is
      gated on the user OBTAINING a non-CRyPTIC source = external wall.)
  (b) INDEL-NORM: tighten the CRyPTIC-parquet baseline by including indel determinants — IF cleanly doable.
- Gates: BV-BRC/web reads auto; in-cwd writes auto; no money/destructive.
