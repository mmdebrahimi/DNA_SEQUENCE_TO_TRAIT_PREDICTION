# recommendation — tb-goldset
- GOLD SET: the machinery + playbook are ready; the binding step is OBTAINING a non-CRyPTIC TB DST+genome
  source (external). Highest-leverage next move: in a FRESH web-enabled session, run the source survey (my
  filter blocked it here) to hand the user 2-3 concrete post-2023 candidate datasets + ENA accessions; then
  the leakage->manifest->score path is a few commands. A ~30-isolate hand-curated first cut is the
  lowest-effort entry.
- INDEL-NORM: defer until either (a) the genomic indel left-alignment normalizer is written, or (b) the
  CRyPTIC EFFECTS-table protein-form match is validated non-circular. Small INH-katG-LoF payoff; not worth a
  wrong-results risk now.
