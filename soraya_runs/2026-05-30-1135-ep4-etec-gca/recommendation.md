# Soraya Recommendation
1. Close ETEC arm: download PMC8085198 Additional File 4/5 (Excel) → read per-strain chromosome accession column. Best done where the file can be opened + parsed (this laptop CAN, via a direct download + pandas/openpyxl — viable next solo action if user wants).
2. Else workhorse enumerates via `datasets`/ENA from its network on sync.
3. Unchanged: USER send Gate B; eval weights ETEC as near-conformance only.
